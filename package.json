{
  "name": "ame-agent-chat",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI Agent (OpenCode) をリッチな UI で安全・低コスト運用する Windows / Linux / macOS 対応ローカル開発環境",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "pnpm -r --if-present --filter @ame-agent-chat/agent-core --filter @ame-agent-chat/frontend test",
    "dev": "node scripts/dev.mjs",
    "build": "pnpm -r build",
    "start": "node scripts/start.mjs",
    "stop": "node scripts/stop.mjs",
    "docs:dev": "pnpm --filter @ame-agent-chat/docs dev",
    "docs:build": "pnpm --filter @ame-agent-chat/docs build",
    "docs:preview": "pnpm --filter @ame-agent-chat/docs preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.19.1"
  }
}
