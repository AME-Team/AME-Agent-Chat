/**
 * Gatekeeper API エントリポイント — Issue #13 で本体実装
 *
 * 要件 #1 §2.1, §3.4, §4.1
 *  - Windows ホスト上の別プロセス (ポート 87880)
 *  - ファイル I/O 制御・承認フロー・パストラバーサル対策
 *  - Execute は完全禁止
 */
export function gatekeeperPlaceholder(): string {
  return 'gatekeeper';
}
