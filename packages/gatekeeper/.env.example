# Gatekeeper API 環境変数 — コピーして .env へ
# Gatekeeper API ポート (要件 #1 §2.6 は 87880 と記載だが TCP 上限超過のため 58780 で運用)
PORT=58780
# バインド先ホスト (ホスト OS 公開時は 0.0.0.0)
HOST=0.0.0.0
# Frontend (CORS 許可オリジン)
CORS_ORIGIN=http://localhost:51730
# SQLite DB パス (既定: gatekeeper/data/ame.db)
AME_DB_PATH=./data/ame.db
